Now I want to upgrade the project to support multiple languages with proper internationalization.

🌍 Languages to support:
1. English (default)
2. Arabic (RTL)
3. Turkish
4. French
5. Spanish
6. Hindi
7. Chinese (Simplified)

🔧 Requirements:

1. Internationalization (i18n):
- Implement a clean i18n system (JSON-based translations preferred)
- All UI text must be translatable (buttons, labels, placeholders, messages, FAQs, etc.)
- Language files should be structured clearly (e.g. /locales/en.json, ar.json, tr.json, etc.)

2. Translations:
- Provide FULL translations for all supported languages
- Do not leave placeholders or partial translations
- Ensure translations are natural and human-like (not robotic)

3. Language Switcher:
- Add a language switcher in the UI (dropdown or toggle)
- Save selected language in localStorage
- Auto-detect browser language on first visit

4. RTL Support (IMPORTANT):
- When Arabic is selected:
  - Change page direction to RTL
  - Align text properly (right-aligned)
  - Adjust layout spacing and UI components accordingly
  - Ensure buttons, inputs, and icons are mirrored correctly if needed

5. SEO for Multi-language:
- Each language must have its own URL structure:
  Example:
  /en/time-zone-converter
  /ar/time-zone-converter
  /tr/time-zone-converter
- Add hreflang tags properly
- Translate meta titles and descriptions for each language

6. UI/UX:
- Ensure fonts support all languages (especially Arabic, Hindi, Chinese)
- Maintain clean and consistent design across languages
- Avoid layout breaking due to long translations

7. Performance:
- Load only required language file (lazy loading)
- Keep translations lightweight

📦 Deliverables:
1. Updated project structure for multilingual support
2. Translation files for ALL languages (complete)
3. Example implementation for one tool page
4. Language switcher component
5. RTL implementation details (CSS changes)
6. SEO setup for multilingual pages

⚠️ Important:
- This is not a demo, build it production-ready
- Focus on scalability (easy to add more languages later)